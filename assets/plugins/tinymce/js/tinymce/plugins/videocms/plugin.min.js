tinymce.PluginManager.add('videocms', function(editor, url) {
    editor.addButton('videocms', {
        title: 'Tambah Video',
        icon: 'media',
        onclick: function() {
            var title = 'Add Video';
            var src = base_url + 'video/index?modals=true'
            $('#general-modal-title').html(title);
            $('#general-modal-iframe').attr('src',src);
            $('#general-modal').modal('show');
        }
    });
});
